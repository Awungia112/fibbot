name: Test FibBot

on: [pull_request, push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Run FibBot
        uses: ./
        with:
          enable_fib: true
          max_threshold: 250
